import { FeedPage } from "@/components/feed-page";

export default function DartPage() {
  return (
    <FeedPage
      type="dart"
      title="DART 호재 공시"
      description="한국 DART RSS에서 기존 Python 분류 기준상 호재 가능성이 있는 공시만 추려서 실시간에 가깝게 보여줍니다."
    />
  );
}
